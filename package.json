{
  "name": "critique-workspace",
  "private": true,
  "workspaces": ["./*"],
  "scripts": {
    "build:comments-widget": "bun build cli/src/agentation-widget.tsx --outfile cli/public/agentation-widget.js --minify --target browser",
    "build:comments-server": "bun run --filter '@critique.work/server' build"
  }
}
