---
'critique': patch
---

Update the README to match the current npm package guidance and supported CLI behavior.

The docs now show `--web` as the primary way to create hosted diff URLs, clarify that local HTML export is not available yet, and point users to the tracked local export request instead of implying a fallback file is generated.
